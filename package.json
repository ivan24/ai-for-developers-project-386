{
  "name": "calendar-booking-workspace",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "corepack pnpm --dir frontend dev --host 0.0.0.0",
    "build": "corepack pnpm run gen-api && corepack pnpm -r build",
    "typecheck": "corepack pnpm run gen-api && corepack pnpm --dir frontend exec tsc -b --pretty false && corepack pnpm --dir packages/api-client exec tsc --pretty false --noEmit",
    "test": "corepack pnpm run gen-api && corepack pnpm --dir frontend test",
    "lint": "corepack pnpm --dir frontend lint",
    "gen-openapi": "node scripts/generate-openapi.mjs",
    "gen-api": "openapi-typescript generated/openapi/calendar.yaml -o packages/api-client/src/generated.ts"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.14.2",
    "@typespec/compiler": "^1.11.0",
    "@typespec/http": "^1.11.0",
    "@typespec/openapi3": "^1.11.0",
    "@typespec/rest": "^0.81.0",
    "openapi-typescript": "^7.13.0"
  }
}
